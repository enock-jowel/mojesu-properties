/**
 * Legacy Cloudflare Pages Functions stubs — unused.
 * Lead APIs live in Next.js App Router under app/api/*.
 * Prefer Vercel (primary) or OpenNext Worker deploy; do not add new handlers here.
 */
export {}
